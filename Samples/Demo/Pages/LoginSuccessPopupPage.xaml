<?xml version="1.0" encoding="utf-8" ?>
<rg:PopupPage
    x:Class="Demo.Pages.LoginSuccessPopupPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:rg="http://rotorgames.com"
    x:Name="ThisPage"
    BackgroundColor="Transparent"
    BackgroundInputTransparent="True"
    CloseWhenBackgroundIsClicked="False"
    HasSystemPadding="False">
    <rg:PopupPage.Animation>
        <rg:MoveAnimation PositionIn="Top" PositionOut="Top" />
    </rg:PopupPage.Animation>
    <StackLayout BackgroundColor="#43A047" VerticalOptions="Start">
        <BoxView HeightRequest="{Binding Source={x:Reference ThisPage}, Path=SystemPadding.Top}" />
        <Label
            Margin="10,0,0,5"
            Text="Login is success"
            TextColor="White"
            VerticalOptions="EndAndExpand" />
    </StackLayout>
</rg:PopupPage>